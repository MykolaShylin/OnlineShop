@model decimal

@if(Model > 0)
{
    <a asp-area="" asp-controller="Basket" asp-action="Checkout" class="btn">
        <img src="/icons/basket_icon.png" width="40">
        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-warning" style="font-size:15px">
            @Model
        </span>
    </a>
}
else
{
    <a href="/basket/checkout"><img src="/icons/basket_icon.png" width="40"></a>
}